# Lab 4 — the ServiceAccount your pod runs as.
# The managed identity and its federated credential already exist for your namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: orders-api
  annotations:
    azure.workload.identity/client-id: <CLIENT-ID>
